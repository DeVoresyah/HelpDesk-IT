import { connect } from '../../../Themes/OsmiProvider'

export default connect({
  container: 'flex full mb-5',
  inputStyle: 'full text-sm rounded-md bg-gray-100 dark:bg-gray-700 py-2 px-3 text-gray-900 dark:text-gray-800',
  error: 'text-red-500 mt-2'
})
